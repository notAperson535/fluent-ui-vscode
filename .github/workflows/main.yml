name: Build
on: workflow_dispatch
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - run: npm install -g vsce
      - run: vsce package
      
      - uses: actions/upload-artifact@v3
        with:
          name: my-artifact
          path: fluent-ui-vscode-*.vsix
